<p id="notice"><%= notice %></p>

<div class="jumbotron">
  <h1 class="display-4">Product: <%= @product.Product %></h1>
  <li>
    <p class="lead"> Organization: <%= @product.Organization %></p>
    <p class="lead">Date: <%= @product.Date %></p>
    <p class="lead">PO_Number: <%= @product.PO_Number %></p>
    <p class="lead">Invoice_Number: <%= @product.Invoice_Number %></p>
    <p class="lead">Invoice_Date: <%= @product.Invoice_Date %></p>
    <p class="lead">Quantity: <%= @product.Quantity %></p>
    <p class="lead">Value:  <%= @product.Value %>($)</p>
    <p class="lead">Weight:  <%= @product.Weight %>(lb)</p>
    <table>
    <tbody>
    <tr>
    <td>
    <button name="button" type="button" class="sub btn btn-default">-</button>
    </td>
    <br/>
    <p>Quantity Tracker</p>
    <td><input name="quantity" type="text" id="1" class="field" value= "<%=@product.Quantity%>" /></td>
    <td>
    <button name="button" type="button" class="add btn btn-default">+</button>
    </td>
    </tr>
    </tbody>
    </table>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>


<script> 
function UpdateCount() {
    var text = "";
    $("#myform li").each(function() {
        var field = $(this).find("input[field]").attr("field");
        var val = $(this).find("input[type=text]").val();
        if(parseInt(val) > 0) {
            text += (text == "" ? "" : ", ") + val + " " + field;
        }
    });
    $(".count-pax").text(text);
}
$('.add').click(function() {
  if (!isNaN($(this).parent().prev().children(".field").val()))
    $(this).parent().prev().children(".field").val(+$(this).parent().prev().children(".field").val() + 1);
  else
    $(this).parent().prev().children(".field").val(0);
});
$('.sub').click(function() {
  if ((!isNaN($(this).parent().next().children(".field").val())) && ($(this).parent().next().children(".field").val() > 0))
    $(this).parent().next().children(".field").val(+$(this).parent().next().children(".field").val() - 1);
  else
    $(this).parent().next().children(".field").val(0);
    //UpdateCount();
    //var number = $(this).parent().next().children(".field").val(+$(this).parent().next().children(".field").val(); // get current number as displayed in template

    //$.ajax({
      //url: products_controller/num, 
      //data: number, 
      //success: success,
      //dataType: "script"
   // });
   //source https://stackoverflow.com/questions/2244862/jquery-plus-minus-incrementer
});

</script>
<br>
  </li>
  <hr class="my-4">
  <p class="lead">
    <%= link_to 'Edit', edit_product_path(@product) %> |
    <%= link_to 'Back', products_path %>
  </p>
</div>